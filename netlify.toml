[build]
  command = "cd frontend && npm run build"
  publish = "frontend/.next"
  base = ""

[build.environment]
  NODE_VERSION = "22.12.0"
  NODE_ENV = "production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
